/**
 * Users routes
 *
 * This file was created by the Express generator and currently contains
 * a placeholder route. Extend this file with user-related routes if you
 * plan to add authentication or user management features.
 */
const express = require('express')
const router = express.Router()

// Placeholder: returns a simple text response. Replace with real handlers.
router.get('/', function (req, res, next) {
  res.send('respond with a resource')
})

module.exports = router
