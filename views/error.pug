extends layout

//- Error page: mirrors example-markup/error.html for basic layout
block content
  h1 Uh Oh! Status: #{err.status || 500}
  if err && err.message
    p #{err.message}
  else
    p Sorry! We couldn't find the page you were looking for.
  br
  p
    a.button(href='/') Home
